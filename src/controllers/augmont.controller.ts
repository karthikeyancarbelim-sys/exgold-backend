// ===============================
// augmont.controller.ts (stub)
// ===============================
import { Request, Response } from "express";

export const buyGold = async (_req: Request, res: Response) => {
  res.json({ message: "Integrate Augmont API here" });
};

export const sellGold = async (_req: Request, res: Response) => {
  res.json({ message: "Integrate Augmont API here" });
};

export const getBalance = async (_req: Request, res: Response) => {
  res.json({ message: "Fetch balance from Augmont" });
};